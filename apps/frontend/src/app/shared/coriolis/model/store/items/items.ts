import { meleeWeapon } from './meleeWeapon';
import { armor } from './armors';
import { quirkyStuff } from './quirkyStuff';

export const items = {
  weapons: {
    melee: meleeWeapon,
  },
  armor,
  quirkyStuff,
};
